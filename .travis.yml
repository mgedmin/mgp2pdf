language: python
dist: xenial
python:
    - 2.7
    - 3.5
    - 3.6
    - 3.7
install:
    - pip install coverage coveralls pytest mock -e .
script:
    - coverage run -m pytest
after_success:
    - coveralls
notifications:
    email: false
